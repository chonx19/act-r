# 🔧 แก้ไขปัญหา GitHub Pages 404 Errors

## ปัญหา: 404 Errors สำหรับ index.tsx, index.css, vite.svg

### สาเหตุ:
- Vite ควรจะแปลง path ใน `index.html` อัตโนมัติเมื่อ build
- แต่บางครั้ง path ไม่ถูกแปลง หรือ base path ไม่ถูกใช้

### วิธีแก้ไข:

#### 1. ตรวจสอบ Base Path
- ตรวจสอบว่า `vite.config.ts` มี `base: '/act-r/'` สำหรับ production
- ตรวจสอบว่า GitHub Actions ตั้งค่า `GITHUB_PAGES=true`

#### 2. ตรวจสอบ Build Output
- ตรวจสอบว่าไฟล์ใน `dist/` มี path ถูกต้อง
- ตรวจสอบว่า `dist/index.html` มี path ที่ถูกต้อง

#### 3. ใช้ Relative Paths
- เปลี่ยนจาก `/index.tsx` เป็น `./index.tsx`
- เปลี่ยนจาก `/index.css` เป็น `./index.css`

#### 4. ตรวจสอบ Public Assets
- ไฟล์ใน `public/` จะถูกคัดลอกไปยัง `dist/` โดยอัตโนมัติ
- ตรวจสอบว่าไฟล์อยู่ใน `public/` folder

---

## วิธีแก้ไขที่แนะนำ:

### 1. ใช้ Vite's HTML Transform
Vite ควรจะแปลง path อัตโนมัติ แต่ถ้าไม่ทำงาน:

```html
<!-- เปลี่ยนจาก -->
<script type="module" src="/index.tsx"></script>

<!-- เป็น -->
<script type="module" src="./index.tsx"></script>
```

### 2. ตรวจสอบ Build
```bash
npm run build
# ตรวจสอบ dist/index.html ว่า path ถูกต้องหรือไม่
```

### 3. ตรวจสอบ GitHub Actions
- ไปที่ Actions tab
- ดู build logs
- ตรวจสอบว่า build สำเร็จ

---

## หมายเหตุ:
- Vite จะแปลง path อัตโนมัติเมื่อ build
- Base path จะถูกใช้สำหรับ assets
- HashRouter ไม่ต้องตั้งค่า base path

