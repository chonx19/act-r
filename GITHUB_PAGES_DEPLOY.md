# 🚀 คู่มือ Deploy บน GitHub Pages

## 📋 ขั้นตอนการ Deploy

### ขั้นตอนที่ 1: สร้าง GitHub Repository

1. **ไปที่ GitHub:**
   - เปิด https://github.com
   - Login เข้าบัญชีของคุณ

2. **สร้าง Repository ใหม่:**
   - คลิก **"+"** (มุมขวาบน) > **"New repository"**
   - **Repository name:** `act` หรือชื่อที่ต้องการ
   - **Description:** (Optional) ACT&R Inventory Management System
   - เลือก **Public** หรือ **Private**
   - **อย่า** check "Initialize with README" (เพราะเรามีโค้ดอยู่แล้ว)
   - คลิก **"Create repository"**

---

### ขั้นตอนที่ 2: Push โค้ดขึ้น GitHub

#### วิธีที่ 1: ใช้ Git Command Line (แนะนำ)

เปิด Terminal/PowerShell ในโฟลเดอร์โปรเจค:

```bash
# 1. ตรวจสอบว่า Git ถูกติดตั้งแล้ว
git --version

# 2. ตั้งค่า Git (ถ้ายังไม่ได้ตั้ง)
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 3. Initialize Git repository (ถ้ายังไม่มี)
git init

# 4. เพิ่มไฟล์ทั้งหมด
git add .

# 5. Commit
git commit -m "Initial commit: ACT&R Inventory System"

# 6. เพิ่ม remote repository
git remote add origin https://github.com/YOUR_USERNAME/act.git
# แทน YOUR_USERNAME ด้วย username ของคุณ

# 7. Push ขึ้น GitHub
git branch -M main
git push -u origin main
```

#### วิธีที่ 2: ใช้ GitHub Desktop

1. ดาวน์โหลดและติดตั้ง [GitHub Desktop](https://desktop.github.com/)
2. Login ด้วย GitHub account
3. File > Add Local Repository
4. เลือกโฟลเดอร์ `d:\act`
5. Commit และ Push

#### วิธีที่ 3: ใช้ VS Code

1. เปิดโฟลเดอร์ `d:\act` ใน VS Code
2. ไปที่ Source Control (Ctrl+Shift+G)
3. คลิก "Initialize Repository"
4. Commit และ Push

---

### ขั้นตอนที่ 3: ตั้งค่า GitHub Pages

1. **ไปที่ Repository บน GitHub:**
   - เปิด https://github.com/YOUR_USERNAME/act

2. **ไปที่ Settings:**
   - คลิก **"Settings"** (แท็บด้านบน)

3. **ไปที่ Pages:**
   - ในเมนูด้านซ้าย คลิก **"Pages"**

4. **ตั้งค่า Source:**
   - **Source:** เลือก **"GitHub Actions"**
   - หรือเลือก **"Deploy from a branch"** > **main** > **/ (root)**

5. **บันทึก:**
   - GitHub จะแสดง URL ของเว็บไซต์:
     - `https://YOUR_USERNAME.github.io/act`
     - หรือ `https://YOUR_USERNAME.github.io/act/` (ถ้าใช้ subfolder)

---

### ขั้นตอนที่ 4: ตรวจสอบ GitHub Actions

1. **ไปที่ Actions:**
   - คลิกแท็บ **"Actions"** ใน repository

2. **ตรวจสอบ Workflow:**
   - ควรเห็น workflow **"Deploy to GitHub Pages"** กำลังรัน
   - รอให้ workflow เสร็จ (ประมาณ 2-5 นาที)

3. **ตรวจสอบ Status:**
   - ✅ สีเขียว = สำเร็จ
   - ❌ สีแดง = มีปัญหา (คลิกดู error)

---

### ขั้นตอนที่ 5: ทดสอบเว็บไซต์

1. **รอ Deployment เสร็จ:**
   - ไปที่ Settings > Pages
   - ดูว่า "Your site is live at..." แสดง URL

2. **เปิดเว็บไซต์:**
   - เปิด URL ที่ได้ (เช่น `https://YOUR_USERNAME.github.io/act`)
   - ทดสอบว่าแอปทำงาน

3. **ตรวจสอบ Excel Export:**
   - เปิด Kanban Board
   - สร้างหรือเปิด PO
   - ทดสอบ Export Excel

---

## 🔧 การตั้งค่าเพิ่มเติม

### ตั้งค่า Custom Domain (Optional)

1. **ใน GitHub Pages Settings:**
   - ไปที่ Settings > Pages
   - ในส่วน "Custom domain" พิมพ์ domain ของคุณ (เช่น `actr.tk`)
   - คลิก **"Save"**

2. **ตั้งค่า DNS:**
   - ไปที่ผู้ให้บริการ domain ของคุณ
   - เพิ่ม DNS records:
     - **Type:** `A`
     - **Name:** `@`
     - **Value:** `185.199.108.153`
     - **Type:** `A`
     - **Name:** `@`
     - **Value:** `185.199.109.153`
     - **Type:** `A`
     - **Name:** `@`
     - **Value:** `185.199.110.153`
     - **Type:** `A`
     - **Name:** `@`
     - **Value:** `185.199.111.153`
     - **Type:** `CNAME`
     - **Name:** `www`
     - **Value:** `YOUR_USERNAME.github.io`

3. **รอ DNS Propagation:**
   - ใช้เวลา 1-24 ชั่วโมง
   - ตรวจสอบที่ https://dnschecker.org

---

## 📝 หมายเหตุสำคัญ

### ✅ ข้อดีของ GitHub Pages

- **ฟรี 100%** - ไม่มีค่าใช้จ่าย
- **HTTPS อัตโนมัติ** - GitHub ให้ SSL certificate ฟรี
- **Auto Deploy** - Deploy อัตโนมัติเมื่อ push code
- **Custom Domain** - ใช้ domain ของตัวเองได้

### ⚠️ ข้อจำกัด

- **Public Repository:** ถ้าใช้ Free plan จะต้องเป็น Public repo
- **Private Repository:** ต้องใช้ GitHub Pro ($4/เดือน) สำหรับ Private repo
- **Build Time:** จำกัด 10 นาทีต่อ build
- **Storage:** จำกัด 1 GB

### 🔒 Security

- **Environment Variables:** อย่า commit `.env.local` ที่มี API keys
- **ไฟล์ `.gitignore`:** ตรวจสอบว่ามี `.env.local` อยู่แล้ว

---

## 🆘 Troubleshooting

### ปัญหา: Workflow ไม่ทำงาน

**แก้ไข:**
1. ตรวจสอบว่าไฟล์ `.github/workflows/deploy.yml` มีอยู่
2. ตรวจสอบว่า push code ขึ้น GitHub แล้ว
3. ไปที่ Settings > Actions > General
4. ตรวจสอบว่า "Workflow permissions" ตั้งค่าเป็น "Read and write permissions"

### ปัญหา: Build ล้มเหลว

**แก้ไข:**
1. ไปที่ Actions > คลิก workflow ที่ล้มเหลว
2. ดู error message
3. ตรวจสอบว่า `package.json` มี dependencies ครบ
4. ลองรัน `npm install` และ `npm run build` ในเครื่องก่อน

### ปัญหา: หน้าเว็บไม่แสดง

**แก้ไข:**
1. ตรวจสอบว่าใช้ **HashRouter** (ใน `App.tsx`)
2. ตรวจสอบว่า build สำเร็จ
3. ตรวจสอบ URL ถูกต้อง (มี `/` ท้าย URL หรือไม่)

### ปัญหา: Excel Export ไม่ทำงาน

**แก้ไข:**
1. ตรวจสอบว่าไฟล์ `public/Excel/RFQ.xlsx` มีอยู่
2. ตรวจสอบ console ใน browser สำหรับ error
3. ตรวจสอบว่า path ใน code ถูกต้อง (`/Excel/RFQ.xlsx`)

---

## 📚 ข้อมูลเพิ่มเติม

- **GitHub Pages Docs:** https://docs.github.com/en/pages
- **GitHub Actions Docs:** https://docs.github.com/en/actions
- **Vite Deployment Guide:** https://vitejs.dev/guide/static-deploy.html

---

## ✅ Checklist

- [ ] สร้าง GitHub Repository
- [ ] Push code ขึ้น GitHub
- [ ] ตั้งค่า GitHub Pages (Source: GitHub Actions)
- [ ] ตรวจสอบ GitHub Actions workflow
- [ ] ทดสอบเว็บไซต์
- [ ] ตรวจสอบ Excel Export
- [ ] (Optional) ตั้งค่า Custom Domain

---

## 🎉 เสร็จแล้ว!

ตอนนี้คุณมี:
- ✅ เว็บแอปที่ deploy บน GitHub Pages
- ✅ URL: `https://YOUR_USERNAME.github.io/act`
- ✅ HTTPS อัตโนมัติ
- ✅ Auto Deploy เมื่อ push code

**หมายเหตุ:** ทุกครั้งที่ push code ใหม่ GitHub จะ build และ deploy อัตโนมัติ!

