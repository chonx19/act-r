# 🔧 แก้ไขปัญหา index.tsx 404

## ปัญหา:
- `GET https://chonx19.github.io/index.tsx net::ERR_ABORTED 404`

## สาเหตุ:
Vite ควรจะแปลง `/index.tsx` ใน `index.html` เป็น JavaScript file เมื่อ build แต่ดูเหมือนจะไม่ทำงาน

## วิธีแก้ไข:

### 1. ตรวจสอบ Build Output
Vite จะแปลง path ใน `index.html` อัตโนมัติเมื่อ build:
- `/index.tsx` → `/act-r/index.js` หรือ `/act-r/assets/index-xxx.js`
- แต่ต้องตั้งค่า base path ถูกต้อง

### 2. ตรวจสอบว่า Build ใช้ Base Path
- ตรวจสอบว่า `GITHUB_PAGES=true` และ `NODE_ENV=production` ถูกตั้งค่าใน GitHub Actions
- ตรวจสอบ build logs ว่า base path ถูกต้อง

### 3. วิธีแก้ไขที่แนะนำ:
Vite จะแปลง path อัตโนมัติเมื่อ build ถ้าเราใช้ absolute path ที่ขึ้นต้นด้วย `/`

แต่ถ้ายังไม่ได้ อาจต้อง:
- ตรวจสอบว่า build สำเร็จ
- ตรวจสอบว่า `dist/index.html` มี path ถูกต้อง
- ลบ console.log ออกจาก vite.config.ts (ไม่จำเป็นใน production)

---

## หมายเหตุ:
- Vite จะแปลง path ใน `index.html` อัตโนมัติเมื่อ build
- Base path จะถูกใช้สำหรับ assets
- HashRouter ไม่ต้องตั้งค่า base path

